---
name: spec-design
description: Draft or refine behavior-focused issues with observable requirements, customer scenarios, edge cases, and scope boundaries.
compatibility: opencode,codex
---

# Spec Design

Use this skill when the user wants to define product behavior, observable requirements, customer scenarios, edge cases, and scope boundaries.

Load and follow the canonical instructions in `../../../plugins/spec/skills/design/SKILL.md`.

Keep shared references reachable from `../../../plugins/spec/references/`, especially:

- `design-template.md`
- `workflow-rules.md`

When the host tool does not support namespaced slash commands, treat `spec-design` as equivalent to `/spec:design`.
